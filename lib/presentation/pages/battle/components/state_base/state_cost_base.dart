import 'package:flutter/material.dart';

class StateCostBase extends StatelessWidget {
  const StateCostBase({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
